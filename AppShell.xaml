<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="NovelBook.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:NovelBook"
    xmlns:views="clr-namespace:NovelBook.Views"
    xmlns:extensions="clr-namespace:NovelBook.Extensions"
    Shell.FlyoutBackgroundColor="{StaticResource BackgroundDark}"
    Shell.FlyoutHeaderBehavior="CollapseOnScroll">

    <!-- Estilo del Shell -->
    <Shell.Resources>
        <ResourceDictionary>
            <Style x:Key="BaseStyle" TargetType="Element">
                <Setter Property="Shell.BackgroundColor" Value="{StaticResource BackgroundDark}" />
                <Setter Property="Shell.ForegroundColor" Value="{StaticResource TextPrimary}" />
                <Setter Property="Shell.TitleColor" Value="{StaticResource TextPrimary}" />
                <Setter Property="Shell.DisabledColor" Value="{StaticResource TextMuted}" />
                <Setter Property="Shell.UnselectedColor" Value="{StaticResource TextSecondary}" />
                <Setter Property="Shell.TabBarBackgroundColor" Value="{StaticResource BackgroundDark}" />
                <Setter Property="Shell.TabBarForegroundColor" Value="{StaticResource TextPrimary}"/>
                <Setter Property="Shell.TabBarUnselectedColor" Value="{StaticResource TextSecondary}"/>
                <Setter Property="Shell.TabBarTitleColor" Value="{StaticResource TextPrimary}"/>
            </Style>
            <Style TargetType="TabBar" BasedOn="{StaticResource BaseStyle}" />
            <Style TargetType="FlyoutItem" BasedOn="{StaticResource BaseStyle}" />
            <Style TargetType="Shell" ApplyToDerivedTypes="True">
                <Setter Property="Shell.ForegroundColor" Value="{StaticResource TextPrimary}" />
                <Setter Property="Shell.BackgroundColor" Value="{StaticResource BackgroundDark}" />
                <Setter Property="Shell.TitleColor" Value="{StaticResource TextPrimary}" />
                <Setter Property="Shell.NavBarHasShadow" Value="True"/>
            </Style>
        </ResourceDictionary>
    </Shell.Resources>

    <!-- Header del menú lateral -->
    <Shell.FlyoutHeader>
        <Grid BackgroundColor="{StaticResource BackgroundMedium}" 
              HeightRequest="200"
              Padding="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Text="📚" 
                   FontSize="60"
                   HorizontalOptions="Center"
                   VerticalOptions="Center"
                   Grid.Row="0"/>

            <Label Text="NOVELBOOK" 
                   FontSize="24"
                   FontAttributes="Bold"
                   TextColor="{StaticResource Primary}"
                   HorizontalOptions="Center"
                   Grid.Row="1"/>

            <Label Text="{extensions:Translate YourPersonalLibrary}"
                   FontSize="14"
                   TextColor="{StaticResource TextSecondary}"
                   HorizontalOptions="Center"
                   Grid.Row="2"
                   Margin="0,0,0,10"/>
        </Grid>
    </Shell.FlyoutHeader>

    <!-- Items principales con TabBar -->
    <TabBar>
        <Tab Title="{extensions:Translate Library}" Icon="library_icon.png" Route="LibraryTab">
            <ShellContent Title="Mi Librería" Route="LibraryPage" ContentTemplate="{DataTemplate views:LibraryPage}"/>
        </Tab>

        <Tab Title="{extensions:Translate Explore}" Icon="explore_icon.png" Route="ExploreTab">
            <ShellContent Title="Explorar Novelas" Route="ExplorePage" ContentTemplate="{DataTemplate views:ExplorePage}"/>
        </Tab>

        <Tab Title="{extensions:Translate History}" Icon="history_icon.png" Route="HistoryTab">
            <ShellContent Title="Historial de Lectura" Route="HistoryPage" ContentTemplate="{DataTemplate views:HistoryPage}"/>
        </Tab>

        <Tab Title="{extensions:Translate More}" Icon="more_icon.png" Route="MoreTab">
            <ShellContent Title="Más Opciones" Route="MorePage" ContentTemplate="{DataTemplate views:MorePage}"/>
        </Tab>
    </TabBar>

    <!-- Items del menú lateral (Flyout) -->
    <FlyoutItem Title="{extensions:Translate Settings}" Icon="settings_icon.png" Route="SettingsTab">
        <ShellContent Title="Configuración" Route="SettingsPage" ContentTemplate="{DataTemplate views:SettingsPage}"/>
    </FlyoutItem>

    <FlyoutItem Title="{extensions:Translate Downloads}" Icon="download_icon.png" Route="DownloadsTab">
        <ShellContent Title="Descargas" Route="DownloadsPage" ContentTemplate="{DataTemplate views:DownloadsPage}"/>
    </FlyoutItem>

    <FlyoutItem Title="{extensions:Translate Statistics}" Icon="stats_icon.png" Route="StatsTab">
        <ShellContent Title="Estadísticas" Route="StatsPage" ContentTemplate="{DataTemplate views:StatsPage}"/>
    </FlyoutItem>

    <!-- Páginas no en Tabs ni Flyout (solo navegación por rutas) -->
    <!-- No van aquí, se registran en AppShell.xaml.cs -->

    <!-- Footer menú -->
    <Shell.FlyoutFooter>
        <VerticalStackLayout Padding="10,10" BackgroundColor="{StaticResource BackgroundMedium}">
            <Button Text="{extensions:Translate Logout}"
                    BackgroundColor="{StaticResource Primary}"
                    TextColor="{StaticResource TextPrimary}"
                    Margin="10"
                    Clicked="OnLogoutClicked"/>
        </VerticalStackLayout>
    </Shell.FlyoutFooter>

</Shell>
