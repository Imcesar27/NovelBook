<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NovelBook.Views.ExplorePage"
             Title="Explorar"
             BackgroundColor="{StaticResource BackgroundDark}">

    <Grid Padding="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Barra de búsqueda con estilo personalizado -->
        <Frame Grid.Row="0" 
               BackgroundColor="{StaticResource BackgroundMedium}"
               CornerRadius="20"
               Padding="5,0"
               Margin="0,5,0,10"
               HasShadow="False">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Label Text="🔍" 
                       FontSize="20"
                       VerticalOptions="Center"
                       Margin="10,0,5,0"
                       Grid.Column="0"/>

                <Entry x:Name="searchEntry"
                       Placeholder="Buscar novelas..."
                       PlaceholderColor="{StaticResource TextMuted}"
                       TextColor="{StaticResource TextPrimary}"
                       BackgroundColor="Transparent"
                       VerticalOptions="Center"
                       Grid.Column="1"
                       TextChanged="OnSearchTextChanged"/>

                <Button Text="⚙️"
                        BackgroundColor="Transparent"
                        TextColor="{StaticResource TextPrimary}"
                        WidthRequest="55"
                        HeightRequest="40"
                        FontSize="20"
                        Grid.Column="2"
                        Clicked="OnFilterClicked"/>
            </Grid>
        </Frame>

        <!-- Categorías populares -->
        <Label Text="Categorías Populares" 
               FontSize="18"
               FontAttributes="Bold"
               TextColor="{StaticResource TextPrimary}"
               Grid.Row="1"
               Margin="5,0,0,5"/>

        <ScrollView Grid.Row="2" 
                    x:Name="CategoryScrollView"
                    Orientation="Horizontal"
                    HeightRequest="50"
                    Margin="0,0,0,15">
            <HorizontalStackLayout x:Name="CategoryButtons" Spacing="10">
                <!-- Los botones se agregan dinámicamente desde el código -->
                <Button Text="🔥 Todas" 
                        BackgroundColor="{StaticResource Primary}"
                        TextColor="White"
                        CornerRadius="15"
                        Padding="15,5"
                        Clicked="OnCategoryClicked"/>
            </HorizontalStackLayout>
        </ScrollView>

        <!-- Grid de novelas con indicador de carga -->
        <Grid Grid.Row="3">
            <!-- Indicador de carga -->
            <ActivityIndicator x:Name="LoadingIndicator"
                               IsRunning="False"
                               IsVisible="False"
                               Color="{StaticResource Primary}"
                               HorizontalOptions="Center"
                               VerticalOptions="CenterAndExpand"
                               Scale="1.5"/>

            <!-- Lista de novelas -->
            <ScrollView>
                <CollectionView x:Name="novelsCollection">
                    <CollectionView.ItemsLayout>
                        <GridItemsLayout Orientation="Vertical" 
                                         Span="2" 
                                         VerticalItemSpacing="10" 
                                         HorizontalItemSpacing="10"/>
                    </CollectionView.ItemsLayout>

                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Frame BackgroundColor="{StaticResource BackgroundMedium}"
                                   CornerRadius="10"
                                   Padding="0"
                                   HasShadow="False">
                                <Frame.GestureRecognizers>
                                    <TapGestureRecognizer Tapped="OnNovelTapped"/>
                                </Frame.GestureRecognizers>

                                <Grid RowSpacing="0">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="200"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Portada con badge de estado -->
                                    <Grid Grid.Row="0">
                                        <Image Source="{Binding CoverImageSource}"
                                               Aspect="AspectFill"/>

                                        <!-- Badge de estado -->
                                        <Frame BackgroundColor="{Binding StatusColor}"
                                               CornerRadius="10"
                                               Padding="8,4"
                                               Margin="5"
                                               HorizontalOptions="Start"
                                               VerticalOptions="Start"
                                               HasShadow="False">
                                            <Label Text="{Binding Status}"
                                                   TextColor="White"
                                                   FontSize="10"
                                                   FontAttributes="Bold"/>
                                        </Frame>
                                    </Grid>

                                    <!-- Información -->
                                    <StackLayout Grid.Row="1" 
                                                 Padding="10" 
                                                 Spacing="5"
                                                 BackgroundColor="{StaticResource BackgroundMedium}">
                                        <Label Text="{Binding Title}"
                                               TextColor="{StaticResource TextPrimary}"
                                               FontAttributes="Bold"
                                               FontSize="14"
                                               LineBreakMode="TailTruncation"
                                               MaxLines="2"/>
                                        <!-- Boton de autor -->
                                        <Label Text="{Binding Author}"
                                               TextColor="{StaticResource TextSecondary}"
                                               FontSize="14"
                                               LineBreakMode="TailTruncation">
                                            <Label.GestureRecognizers>
                                                <TapGestureRecognizer Tapped="OnAuthorTapped" 
                                                                      CommandParameter="{Binding Author}"/>
                                            </Label.GestureRecognizers>
                                        </Label>
                                        <Grid ColumnSpacing="10">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <Label Grid.Column="0"
                                                   TextColor="{StaticResource Primary}"
                                                   FontSize="12">
                                                <Label.FormattedText>
                                                    <FormattedString>
                                                        <Span Text="⭐ "/>
                                                        <Span Text="{Binding Rating}"/>
                                                    </FormattedString>
                                                </Label.FormattedText>
                                            </Label>

                                            <Label Grid.Column="1"
                                                   TextColor="{StaticResource TextSecondary}"
                                                   FontSize="12">
                                                <Label.FormattedText>
                                                    <FormattedString>
                                                        <Span Text="📖 "/>
                                                        <Span Text="{Binding Chapters}"/>
                                                        <Span Text=" cap"/>
                                                    </FormattedString>
                                                </Label.FormattedText>
                                            </Label>
                                        </Grid>
                                    </StackLayout>
                                </Grid>
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </ScrollView>
        </Grid>
    </Grid>
</ContentPage>