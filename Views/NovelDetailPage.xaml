<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:extensions="clr-namespace:NovelBook.Extensions"
             x:Class="NovelBook.Views.NovelDetailPage"
             Title="{extensions:Translate NovelDetails}">

    <ScrollView>
        <VerticalStackLayout Spacing="0">
            <!-- Header con imagen de fondo -->
            <Grid HeightRequest="300">
                <!-- Imagen de fondo -->
                <Image Source="{Binding CoverImageSource}"
                       Aspect="AspectFill"
                       Opacity="0.3"/>

                <!-- Gradiente oscuro -->
                <BoxView>
                    <BoxView.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                            <GradientStop Color="Transparent" Offset="0"/>
                        <GradientStop Color="{AppThemeBinding Light={StaticResource BackgroundDarkLight}, Dark={StaticResource BackgroundDark}}" Offset="1"/>
                    </LinearGradientBrush>
                    </BoxView.Background>
                </BoxView>

                <!-- Contenido sobre la imagen -->
                <Grid Padding="20" VerticalOptions="End">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Portada del libro -->
                    <Frame Grid.Column="0"
                           CornerRadius="10"
                           Padding="0"
                           HasShadow="True"
                           HeightRequest="180"
                           WidthRequest="120">
                        <Image Source="{Binding CoverImageSource}"
                               Aspect="AspectFill"/>
                    </Frame>

                    <!-- Informacion basica -->
                    <VerticalStackLayout Grid.Column="1" 
                                       Padding="15,0,0,0"
                                       Spacing="8"
                                       VerticalOptions="End">
                        <Label Text="{Binding Title}"
                               FontSize="20"
                               FontAttributes="Bold"
                               TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimary}}"
                               LineBreakMode="WordWrap"/>

                        <Label Text="{Binding Author}"
                               FontSize="14"
                               TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondary}}"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Rating -->
                            <Label Grid.Column="0" 
                                   TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimary}}"
                                   Margin="0,0,15,0">
                                <Label.FormattedText>
                                    <FormattedString>
                                        <Span Text="⭐ " />
                                        <Span Text="{Binding Rating}" FontAttributes="Bold"/>
                                    </FormattedString>
                                </Label.FormattedText>
                            </Label>

                            <!-- Capítulos -->
                            <Label Grid.Column="1"
                                   TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondary}}"
                                   Margin="0,0,15,0">
                                <Label.FormattedText>
                                    <FormattedString>
                                        <Span Text="📖 " />
                                        <Span Text="{Binding ChapterCount}" />
                                        <Span Text=" " />
                                        <Span Text="{extensions:Translate Chapters}" />
                                    </FormattedString>
                                </Label.FormattedText>
                            </Label>

                            <!-- Botón de reseñas -->
                            <Button Grid.Column="3"
                                    x:Name="ReviewsButton"
                                    BackgroundColor="Transparent"
                                    TextColor="{StaticResource Primary}"
                                    FontSize="12"
                                    Padding="10,5"
                                    Clicked="OnReviewsClicked">
                                <Button.Text>
                                    <MultiBinding StringFormat="{extensions:Translate ViewReviews}">
                                        <Binding Path="ReviewCount" FallbackValue="0"/>
                                    </MultiBinding>
                                </Button.Text>
                            </Button>

                        </Grid>

                        <Frame BackgroundColor="{Binding StatusColor}"
                               CornerRadius="10"
                               Padding="10,5"
                               HorizontalOptions="Start">
                            <Label Text="{Binding Status}"
                                   TextColor="White"
                                   FontSize="12"
                                   FontAttributes="Bold"/>
                        </Frame>
                    </VerticalStackLayout>
                </Grid>
            </Grid>

            <!-- Botones de accion principales -->
            <Grid Padding="20,15" ColumnSpacing="15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        x:Name="ReadButton"
                        Text="{extensions:Translate StartReading}"
                        BackgroundColor="{StaticResource Primary}"
                        TextColor="White"
                        FontAttributes="Bold"
                        CornerRadius="20"
                        HeightRequest="50"
                        Clicked="OnReadClicked">
                    <Button.Shadow>
                        <Shadow Brush="Black" 
                                Offset="0,2" 
                                Radius="4" 
                                Opacity="0.2"/>
                    </Button.Shadow>
                </Button>

                <Button Grid.Column="1"
                        x:Name="AddToLibraryButton"
                        Text="{extensions:Translate AddToLibrary}"
                        BackgroundColor="{AppThemeBinding Light={StaticResource BackgroundMediumLight}, Dark={StaticResource BackgroundMedium}}"
                        TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimary}}"
                        FontAttributes="Bold"
                        CornerRadius="20"
                        HeightRequest="50"
                        Clicked="OnAddToLibraryClicked">
                    <Button.Shadow>
                        <Shadow Brush="Black" 
                                Offset="0,2" 
                                Radius="4" 
                                Opacity="0.1"/>
                    </Button.Shadow>
                </Button>
            </Grid>

            <!-- Tabs de informacion -->
        <Grid BackgroundColor="{AppThemeBinding Light={StaticResource BackgroundMediumLight}, Dark={StaticResource BackgroundMedium}}" 
                  HeightRequest="50">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="SynopsisTab"
                        Grid.Column="0"
                        Text="{extensions:Translate Synopsis}"
                        BackgroundColor="Transparent"
                        TextColor="{StaticResource Primary}"
                        FontAttributes="Bold"
                        Clicked="OnSynopsisTabClicked"/>

                <Button x:Name="ChaptersTab"
                        Grid.Column="1"
                        Text="{extensions:Translate Chapters}"
                        BackgroundColor="Transparent"
                        TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondary}}"
                        Clicked="OnChaptersTabClicked"/>

                <Button x:Name="DetailsTab"
                        Grid.Column="2"
                        Text="{extensions:Translate Details}"
                        BackgroundColor="Transparent"
                        TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondary}}"
                        Clicked="OnDetailsTabClicked"/>
            </Grid>

            <!-- Contenido de los tabs -->
        <Frame BackgroundColor="{AppThemeBinding Light={StaticResource BackgroundDarkLight}, Dark={StaticResource BackgroundDark}}"
                   Padding="20"
                   CornerRadius="0">
                <Grid>
                    <!-- Sinopsis (visible por defecto) -->
                    <VerticalStackLayout x:Name="SynopsisContent" 
                                       Spacing="15">
                        <Label Text="{extensions:Translate Synopsis}"
                               FontSize="18"
                               FontAttributes="Bold"
                               TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimary}}"/>

                        <Label Text="{Binding Synopsis}"
                               TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondary}}"
                               LineHeight="1.5"
                               LineBreakMode="WordWrap"/>

                        <!-- Tags/Generos -->
                        <Label Text="{extensions:Translate Genres}"
                               FontSize="16"
                               FontAttributes="Bold"
                               TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimary}}"
                               Margin="0,10,0,0"/>

                        <FlexLayout x:Name="GenresLayout"
                                  Wrap="Wrap"
                                  Direction="Row"
                                  JustifyContent="Start"
                                  AlignItems="Start">
                            <!-- Los generos se agregaran dinamicamente -->
                        </FlexLayout>

                        <!-- ========== ETIQUETAS DE USUARIOS ========== -->
                        <Grid ColumnDefinitions="*,Auto" Margin="0,20,0,0">
                            <Label Text="{extensions:Translate Tags}"
                                   FontSize="16"
                                   FontAttributes="Bold"
                                   TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimary}}"
                                   VerticalOptions="Center"/>
                            <Button x:Name="AddTagButton"
                                    Grid.Column="1"
                                    Text="+ Agregar"
                                    FontSize="12"
                                    BackgroundColor="{StaticResource Primary}"
                                    TextColor="White"
                                    CornerRadius="12"
                                    Padding="12,5"
                                    Clicked="OnAddTagClicked"/>
                        </Grid>

                        <!-- Contenedor de etiquetas -->
                        <FlexLayout x:Name="TagsLayout"
                                    Wrap="Wrap"
                                    Direction="Row"
                                    JustifyContent="Start"
                                    AlignItems="Start"
                                    Margin="0,10,0,0">
                            <!-- Las etiquetas se agregan dinámicamente -->
                        </FlexLayout>

                        <!-- Mensaje cuando no hay etiquetas -->
                        <Label x:Name="NoTagsLabel"
                               Text="{extensions:Translate AddFirstTag}"
                               FontSize="12"
                               TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondary}}"
                               IsVisible="False"
                               Margin="0,5,0,0"/>
                    </VerticalStackLayout>

                    <!-- Capitulos (oculto inicialmente) -->
                    <VerticalStackLayout x:Name="ChaptersContent" 
                                       IsVisible="False"
                                       Spacing="10">
                        <!-- Barra de busqueda y filtros -->
                        <SearchBar Placeholder="{extensions:Translate SearchChapter}"
                                   PlaceholderColor="{AppThemeBinding Light={StaticResource TextMutedLight}, Dark={StaticResource TextMuted}}"
                                   TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimary}}"
                                   BackgroundColor="{AppThemeBinding Light={StaticResource BackgroundMediumLight}, Dark={StaticResource BackgroundMedium}}"
                                   TextChanged="OnChapterSearchTextChanged"/>

                        <HorizontalStackLayout Spacing="10" Margin="0,0,0,10">
                            <Button x:Name="AllChaptersButton"
                                    Text="{extensions:Translate AllChapters}"
                                    BackgroundColor="{StaticResource Primary}"
                                    TextColor="White"
                                    CornerRadius="15"
                                    Padding="15,5"
                                    FontSize="12"
                                    Clicked="OnChapterFilterClicked"/>
                            <Button x:Name="UnreadButton"
                                    Text="{extensions:Translate Unread}"
                                    BackgroundColor="{AppThemeBinding Light={StaticResource BackgroundMediumLight}, Dark={StaticResource BackgroundMedium}}"
                                    TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondary}}"
                                    CornerRadius="15"
                                    Padding="15,5"
                                    FontSize="12"
                                    Clicked="OnChapterFilterClicked"/>
                            <Button x:Name="ReverseOrderButton"
                                    Text="{extensions:Translate ReverseOrder}"
                                    BackgroundColor="{AppThemeBinding Light={StaticResource BackgroundMediumLight}, Dark={StaticResource BackgroundMedium}}"
                                    TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondary}}"
                                    CornerRadius="15"
                                    Padding="10,5"
                                    FontSize="12"
                                    Clicked="OnChapterFilterClicked"/>
                        </HorizontalStackLayout>

                        <!-- Lista de capítulos -->
                        <CollectionView x:Name="ChaptersList"
                                        HeightRequest="400">
                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                <Frame BackgroundColor="{AppThemeBinding Light={StaticResource BackgroundMediumLight}, Dark={StaticResource BackgroundMedium}}"
                                           CornerRadius="10"
                                           Padding="15,10"
                                           Margin="0,2">
                                        <Frame.GestureRecognizers>
                                            <TapGestureRecognizer Tapped="OnChapterTapped"/>
                                        </Frame.GestureRecognizers>

                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <VerticalStackLayout Grid.Column="0">
                                                <Label Text="{Binding Title}"
                                                       TextColor="{Binding TextColor}"
                                                       FontSize="14"/>
                                                <Label Text="{Binding Date}"
                                                       TextColor="{AppThemeBinding Light={StaticResource TextMutedLight}, Dark={StaticResource TextMuted}}"
                                                       FontSize="11"/>
                                            </VerticalStackLayout>

                                            <Label Grid.Column="1"
                                                   Text="→"
                                                   TextColor="{StaticResource TextSecondary}"
                                                   FontSize="20"
                                                   VerticalOptions="Center"/>
                                        </Grid>
                                    </Frame>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </VerticalStackLayout>

                    <!-- Detalles (oculto inicialmente) -->
                    <Grid x:Name="DetailsContent" 
                          IsVisible="False"
                          RowSpacing="10"
                          ColumnSpacing="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Label Grid.Row="0" Grid.Column="0"
                               Text="{extensions:Translate Author}"
                               TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondary}}"
                               FontAttributes="Bold"/>
                        <!--Boton autor-->
                        <Label Grid.Row="0" Grid.Column="1"
                               Text="{Binding Author}"
                               TextColor="{StaticResource Primary}"
                               TextDecorations="Underline">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnAuthorTapped"/>
                            </Label.GestureRecognizers>
                        </Label>

                        <Label Grid.Row="1" Grid.Column="0"
                               Text="{extensions:Translate Status}"
                               TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondary}}"
                               FontAttributes="Bold"/>
                        <Label Grid.Row="1" Grid.Column="1"
                               Text="{Binding Status}"
                               TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimary}}"/>

                        <Label Grid.Row="2" Grid.Column="0"
                               Text="{extensions:Translate Chapters}"
                               TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondary}}"
                               FontAttributes="Bold"/>
                        <Label Grid.Row="2" Grid.Column="1"
                               Text="{Binding ChapterCount}"
                               TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimary}}"/>

                        <Label Grid.Row="3" Grid.Column="0"
                               Text="{extensions:Translate LastUpdate}"
                               TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondary}}"
                               FontAttributes="Bold"/>
                        <Label Grid.Row="3" Grid.Column="1"
                               Text="{Binding LastUpdated}"
                               TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimary}}"/>

                        <Label Grid.Row="4" Grid.Column="0"
                               Text="{extensions:Translate Views}"
                               TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondary}}"
                               FontAttributes="Bold"/>
                        <Label Grid.Row="4" Grid.Column="1"
                               Text="{Binding Views}"
                               TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimary}}"/>
                    </Grid>
                </Grid>
            </Frame>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>